<%= form_for @shortening, remote: true do |f| %>
  <%= f.label :url, "paste (ctrl+v) the URL here:" %><br/>
  <%= f.text_field :url %>
<% end %>

<br/>

<%= form_tag '#', remote: true do |f| %>
  <%= label_tag :shortened_url, "copy (ctrl+c) the shortened URL here:" %><br/>
  <%= hidden_field_tag :root_url, root_url %>
  <%= text_field_tag :shortened_url %>
<% end %>
